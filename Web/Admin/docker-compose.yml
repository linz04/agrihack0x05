version: '2'
services:
  sql-1-mysql:
    image: mysql:8.0
    container_name: mysql-server-1-80
    command: --default-authentication-plugin=mysql_native_password
#        working_dir: /application
    volumes:
      - .:/application
      - ./db/db.sql:/docker-entrypoint-initdb.d/db.sql
    restart: always
    environment:
      - MYSQL_ROOT_PASSWORD=r00t3dRah4551a
      - MYSQL_DATABASE=chall
      - MYSQL_USER=db_user
      - MYSQL_PASSWORD=p4s5w0RDR4h451A
    ports:
      - "3306:3306"
  sql-1-website:
    container_name: php72-1
    build:
      context: ./
    volumes:
      - ./src/*:/var/www/html/*
    ports:
      - "16009:80"
    depends_on:
      - sql-1-mysql
