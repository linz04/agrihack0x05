version: '3'
services:
  hacktoday:
    container_name: passwordcracker 
    restart: always
    build:
      context: .
      args:
              binary: nc.py
    ports:
      - "16001:5000" # exposed:local
    read_only: true